name: Update README with Repositories

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Fetch Repositories and Update README
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const username = context.repo.owner;
            
            const { data: repos } = await github.rest.repos.listForUser({
              username: username,
              sort: 'updated',
              per_page: 50, // প্রফেশনাল প্রোফাইলের জন্য সেরা ৫০টি যথেষ্ট
              type: 'public'
            });
            
            // ফিল্টারিং: নিজের রিপোজিটরি এবং ফর্ক করা প্রজেক্ট বাদ দেওয়া
            const filteredRepos = repos.filter(repo => repo.name !== username && !repo.fork);
            
            let projectsHtml = '<div align="center">\n\n';
            projectsHtml += '<h2>🗂️ My Open Source Projects</h2>\n\n';
            projectsHtml += '<table>\n';
            
            // ২ কলামে সাজানো (Responsive Look)
            for (let i = 0; i < filteredRepos.length; i += 2) {
              projectsHtml += '<tr>\n';
              for (let j = 0; j < 2; j++) {
                const repo = filteredRepos[i + j];
                if (repo) {
                  const description = repo.description || 'No description available';
                  
                  // 'pin' এর বদলে 'repo' কার্ড ব্যবহার করা হয়েছে যা সবার জন্য কাজ করবে
                  const cardUrl = `https://github-readme-stats.vercel.app/api/pin/?username=${username}&repo=${repo.name}&theme=tokyonight&show_owner=false`;
                  
                  projectsHtml += `<td width="50%" align="center" style="border: none;">
                  <a href="${repo.html_url}" target="_blank">
                    <img src="https://github-readme-stats.vercel.app/api/pin/?username=${username}&repo=${repo.name}&theme=tokyonight" alt="${repo.name}" />
                  </a>
                  </td>\n`;
                } else {
                  projectsHtml += '<td width="50%" style="border: none;"></td>\n';
                }
              }
              projectsHtml += '</tr>\n';
            }
            
            projectsHtml += '</table>\n\n';
            projectsHtml += `<sub>✨ Last Synchronized: ${new Date().toLocaleString('en-GB', { timeZone: 'Asia/Dhaka', dateStyle: 'medium', timeStyle: 'short' })} (BST)</sub>\n\n`;
            projectsHtml += '</div>\n';
            
            let readme = fs.readFileSync('README.md', 'utf8');
            const startMarker = '';
            const endMarker = '';
            const startIndex = readme.indexOf(startMarker);
            const endIndex = readme.indexOf(endMarker);
            
            if (startIndex !== -1 && endIndex !== -1) {
              const before = readme.substring(0, startIndex + startMarker.length);
              const after = readme.substring(endIndex);
              readme = before + '\n' + projectsHtml + after;
              fs.writeFileSync('README.md', readme);
              console.log('✅ Success: README updated!');
            } else {
              console.log('⚠️ Error: HTML Markers not found in README.md');
            }
      
      - name: Commit and Push Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Auto-update: Refreshed project gallery" && git push)
